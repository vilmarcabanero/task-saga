import { call, put, takeLatest } from 'redux-saga/effects';
import createAPI from 'api/createAPI';
import Actions from './actions';

function* {{ camelCase name }}Default() {
  yield call();
}

function* makeGet{{ properCase name }}() {
  const api = yield createAPI();
  const response = yield call(api.call, 'get{{ properCase name }}');
  yield put(Actions.Creators.set{{ properCase name }}(response.data));
}

export default function* {{ camelCase name }}Saga() {
  yield takeLatest(Actions.Types.DEFAULT, {{ camelCase name }}Default);
  yield takeLatest(Actions.Types.GET_{{ constantCase name }}, makeGet{{ properCase name }});
}
